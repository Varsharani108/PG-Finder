import { useEffect, useState } from "react";
import {
  BarChart3, Building2, CalendarCheck2, CircleDollarSign,
  Eye, LayoutDashboard, Mail, MessageSquare, Pencil, Plus, Save, Star,
  Trash2, UserRound, Users, X,
} from "lucide-react";
import DashboardShell from "./DashboardShell.jsx";
import OwnerPropertiesPanel from "../../components/owner/OwnerPropertiesPanel.jsx";
import { createProperty, deleteProperty, updateProperty } from "../../api/propertyApi.js";
import { getOwnerDashboard, updateBookingStatus, updateInquiryStatus } from "../../api/ownerApi.js";
import { useAuth } from "../../context/AuthContext.jsx";

const emptyForm = { name: "", description: "", city: "", area: "", location: "", college: "", monthlyRent: "", price: "", totalRooms: "", availableRooms: "", roomType: "", genderPreference: "co-living", facilities: [], food: [], foodIncluded: false, distanceFromCollege: "", images: [] };
const propertyDisplayStatus = (property) => {
  const verificationStatus = String(property.verificationStatus || "").toLowerCase();
  if (verificationStatus === "pending") return "Pending Verification";
  if (verificationStatus === "rejected") return "Rejected";
  if (verificationStatus === "verified" && String(property.status || "").toLowerCase() === "active") return "Active";
  return String(property.status || "").toLowerCase() === "active" ? "Active" : "Inactive";
};
const tabs = [
  ["overview", "Overview", LayoutDashboard],
  ["properties", "My Properties", Building2],
  ["inquiries", "Inquiries", MessageSquare],
  ["bookings", "Bookings / Tenants", CalendarCheck2],
  ["reviews", "Reviews", Star],
  ["profile", "Profile", UserRound],
];

function Notice({ notice, onClose }) {
  if (!notice) return null;
  return <div className={`fixed right-4 top-20 z-50 flex max-w-sm items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium text-white shadow-xl ${notice.type === "error" ? "bg-red-600" : "bg-[#0e7c74]"}`} role="status"><span>{notice.message}</span><button onClick={onClose} aria-label="Close notification"><X size={16} /></button></div>;
}

function Metric({ icon: Icon, label, value, onClick, tone }) {
  return <button onClick={onClick} className="dashboard-card w-full rounded-xl border border-primary/10 bg-white p-5 text-left shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><span className={`inline-flex rounded-xl p-3 ${tone}`}><Icon size={19} /></span><p className="mt-4 text-sm text-primary/60">{label}</p><p className="mt-1 text-2xl font-bold text-primary">{value}</p></button>;
}

const dateLabel = (value) => value ? new Date(value).toLocaleDateString() : "Date unavailable";

function OwnerOverview({ properties, inquiries, bookings, reviews, setTab }) {
  const activeProperties = properties.filter((property) => property.status === "Active").length;
  const pendingProperties = properties.filter((property) => property.verificationStatus === "pending").length;
  const pendingBookings = bookings.filter((booking) => booking.status === "Pending").length;
  const activeTenants = bookings.filter((booking) => ["Confirmed", "Active"].includes(booking.status)).length;
  const newInquiries = inquiries.filter((inquiry) => inquiry.status === "New").length;
  const activity = [...inquiries.map((item) => ({ date: item.updatedAt || item.createdAt, label: `Inquiry for ${item.property?.name || "your property"}`, status: item.status })), ...bookings.map((item) => ({ date: item.updatedAt || item.createdAt, label: `Booking for ${item.property?.name || "your property"}`, status: item.status }))].sort((a, b) => new Date(b.date) - new Date(a.date)).slice(0, 6);
  return <><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={Building2} label="Total Properties" value={properties.length} onClick={() => setTab("properties")} tone="bg-blue-50 text-blue-700" /><Metric icon={Building2} label="Active Properties" value={activeProperties} onClick={() => setTab("properties")} tone="bg-green-50 text-green-700" /><Metric icon={Building2} label="Pending Verification" value={pendingProperties} onClick={() => setTab("properties")} tone="bg-amber-50 text-amber-700" /><Metric icon={CalendarCheck2} label="Total Bookings" value={bookings.length} onClick={() => setTab("bookings")} tone="bg-indigo-50 text-indigo-700" /><Metric icon={CalendarCheck2} label="Pending Bookings" value={pendingBookings} onClick={() => setTab("bookings")} tone="bg-orange-50 text-orange-700" /><Metric icon={Users} label="Active Tenants" value={activeTenants} onClick={() => setTab("bookings")} tone="bg-green-50 text-green-700" /><Metric icon={MessageSquare} label="New Inquiries" value={newInquiries} onClick={() => setTab("inquiries")} tone="bg-purple-50 text-purple-700" /></div><section className="mt-6 rounded-xl border border-primary/10 bg-white p-5"><h2 className="font-semibold text-primary">Recent activity</h2>{activity.length ? <div className="mt-4 space-y-3">{activity.map((item, index) => <div key={`${item.date}-${index}`} className="flex items-center justify-between gap-3 rounded-lg bg-primary/5 p-3"><div><p className="text-sm font-medium text-primary">{item.label}</p><p className="text-xs text-primary/55">{dateLabel(item.date)}</p></div><span className="text-xs font-semibold text-primary/60">{item.status}</span></div>)}</div> : <p className="mt-4 text-sm text-primary/60">No recent activity yet.</p>}</section><section className="mt-6 rounded-xl bg-primary p-5 text-white"><div className="flex items-center justify-between"><div><p className="text-sm text-white/60">Owner performance</p><p className="mt-2 text-3xl font-bold">{reviews.length ? (reviews.reduce((sum, review) => sum + review.rating, 0) / reviews.length).toFixed(1) : "-"}</p><p className="mt-1 text-sm text-white/70">Average review rating</p></div><BarChart3 /></div></section></>;
}

function OwnerInquiries({ inquiries, changeInquiryStatus }) {
  const [selected, setSelected] = useState(null);
  return <section className="rounded-xl border border-primary/10 bg-white p-5"><div className="mb-5"><h2 className="text-xl font-semibold text-primary">Inquiries</h2><p className="mt-1 text-sm text-primary/60">Messages from users about your properties.</p></div>{inquiries.length ? <div className="space-y-3">{inquiries.map((item) => <div key={item._id} className="rounded-lg border border-primary/10 p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-medium text-primary">{item.tenant?.name || "Unknown user"}</p><p className="text-sm text-primary/60">{item.property?.name || "Property removed"} · {dateLabel(item.createdAt)}</p><p className="mt-2 text-sm text-primary/70">{item.message}</p>{item.adminReply && <p className="mt-2 text-sm text-accent">Admin reply: {item.adminReply}</p>}</div><span className="rounded-full bg-primary/5 px-3 py-1 text-xs font-semibold text-primary">{item.status}</span></div><div className="mt-3 flex flex-wrap gap-2"><button onClick={() => setSelected(item)} className="inline-flex items-center gap-1 rounded-lg border border-primary/15 px-3 py-2 text-xs font-semibold"><Eye size={14} /> View</button><a href={`mailto:${item.tenant?.email || ""}?subject=Regarding ${encodeURIComponent(item.property?.name || "your inquiry")}`} className="inline-flex items-center gap-1 rounded-lg bg-accent px-3 py-2 text-xs font-semibold text-white"><Mail size={14} /> Reply</a>{item.status !== "Closed" && <><button onClick={() => changeInquiryStatus(item._id, "Viewed")} className="rounded-lg border border-amber-300 px-3 py-2 text-xs font-semibold text-amber-700">Mark In Progress</button><button onClick={() => changeInquiryStatus(item._id, "Closed")} className="rounded-lg bg-emerald-700 px-3 py-2 text-xs font-semibold text-white">Resolve</button></>}</div></div>)}</div> : <p className="py-8 text-center text-sm text-primary/60">No enquiries yet. New enquiries from users will appear here.</p>}{selected && <div className="fixed inset-0 z-40 flex items-center justify-center bg-primary/40 p-4"><div className="w-full max-w-lg rounded-xl bg-white p-6"><div className="flex items-center justify-between"><h3 className="text-lg font-semibold text-primary">Inquiry details</h3><button onClick={() => setSelected(null)} aria-label="Close inquiry details"><X size={18} /></button></div><p className="mt-4 text-sm text-primary/70">{selected.message}</p><p className="mt-2 text-xs text-primary/55">Received {dateLabel(selected.createdAt)} · {selected.status}</p></div></div>}</section>;
}

function OwnerBookings({ bookings, changeBookingStatus }) {
  const [selected, setSelected] = useState(null);
  return <section className="rounded-xl border border-primary/10 bg-white p-5"><h2 className="text-xl font-semibold text-primary">Bookings & Tenants</h2><p className="mt-1 text-sm text-primary/60">Bookings belonging to your properties.</p>{bookings.length ? <div className="mt-5 space-y-3">{bookings.map((booking) => <div key={booking._id} className="rounded-lg border border-primary/10 p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-medium text-primary">{booking.user?.name || booking.tenant?.name || "Unknown user"}</p><p className="text-sm text-primary/60">{booking.property?.name || "Property removed"}</p><p className="text-xs text-primary/50">Booking {dateLabel(booking.bookingDate || booking.createdAt)}{booking.moveInDate ? ` · Move in ${dateLabel(booking.moveInDate)}` : ""}</p></div><div className="text-right"><span className="rounded-full bg-primary/5 px-3 py-1 text-xs font-semibold text-primary">{booking.status}</span><strong className="mt-2 block text-primary">₹{(booking.amount || booking.rent || 0).toLocaleString("en-IN")}</strong></div></div><div className="mt-3 flex flex-wrap gap-2"><button onClick={() => setSelected(booking)} className="inline-flex items-center gap-1 rounded-lg border border-primary/15 px-3 py-2 text-xs font-semibold"><Eye size={14} /> View</button>{booking.status === "Pending" && <><button onClick={() => changeBookingStatus(booking._id, "Confirmed")} className="rounded-lg bg-emerald-700 px-3 py-2 text-xs font-semibold text-white">Confirm</button><button onClick={() => changeBookingStatus(booking._id, "Rejected")} className="rounded-lg bg-red-600 px-3 py-2 text-xs font-semibold text-white">Reject</button><button onClick={() => changeBookingStatus(booking._id, "Cancelled")} className="rounded-lg border border-red-200 px-3 py-2 text-xs font-semibold text-red-600">Cancel</button></>}{booking.status === "Confirmed" && <button onClick={() => changeBookingStatus(booking._id, "Cancelled")} className="rounded-lg border border-red-200 px-3 py-2 text-xs font-semibold text-red-600">Cancel</button>}</div></div>)}</div> : <p className="py-8 text-center text-sm text-primary/60">No bookings or tenants found.</p>}{selected && <div className="fixed inset-0 z-40 flex items-center justify-center bg-primary/40 p-4"><div className="w-full max-w-lg rounded-xl bg-white p-6"><div className="flex items-center justify-between"><h3 className="text-lg font-semibold text-primary">Booking details</h3><button onClick={() => setSelected(null)} aria-label="Close booking details"><X size={18} /></button></div><p className="mt-4 text-sm text-primary/70">{selected.property?.name || "Property removed"} · {selected.status}</p><p className="mt-2 text-sm text-primary/70">Amount: ₹{(selected.amount || selected.rent || 0).toLocaleString("en-IN")}</p></div></div>}</section>;
}

function OwnerReviews({ reviews }) {
  return <section className="rounded-xl border border-primary/10 bg-white p-5"><h2 className="text-xl font-semibold text-primary">Reviews</h2><p className="mt-1 text-sm text-primary/60">Reviews for your properties.</p>{reviews.length ? <div className="mt-5 space-y-3">{reviews.map((review) => <article key={review._id} className="rounded-lg border border-primary/10 p-4"><div className="flex flex-wrap justify-between gap-3"><div><p className="font-medium text-primary">{review.tenant?.name || "User"}</p><p className="text-sm text-primary/60">{review.property?.name || "Property removed"} · {dateLabel(review.createdAt)}</p></div><span className="font-semibold text-accent">{review.rating}/5</span></div><p className="mt-3 text-sm text-primary/70">{review.comment || "No written review."}</p>{review.tenant?.email && <a href={`mailto:${review.tenant.email}?subject=Regarding your review`} className="mt-3 inline-flex items-center gap-1 text-sm font-semibold text-accent"><Mail size={15} /> Reply</a>}</article>)}</div> : <p className="py-8 text-center text-sm text-primary/60">No reviews found.</p>}</section>;
}

function OwnerProfile({ user, profileForm, setProfileForm, saveProfile, profileSaving }) {
  return <section className="max-w-2xl rounded-xl border border-primary/10 bg-white p-5"><h2 className="text-xl font-semibold text-primary">Profile</h2><p className="mt-1 text-sm text-primary/60">Update your owner account details.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><p className="text-sm text-primary/70"><strong className="block text-primary">Role</strong>Owner</p><p className="text-sm text-primary/70"><strong className="block text-primary">Verification status</strong>{user?.verificationStatus || "pending"}</p></div><form onSubmit={saveProfile} className="mt-5 space-y-4"><label className="block text-sm font-medium text-primary">Full name<input required value={profileForm.name} onChange={(e) => setProfileForm({ ...profileForm, name: e.target.value })} className="mt-2 w-full rounded-lg border border-primary/15 px-3 py-2.5" /></label><label className="block text-sm font-medium text-primary">Email<input required type="email" value={profileForm.email || user?.email || ""} onChange={(e) => setProfileForm({ ...profileForm, email: e.target.value })} className="mt-2 w-full rounded-lg border border-primary/15 px-3 py-2.5" /></label><label className="block text-sm font-medium text-primary">Phone<input required pattern="[0-9]{10}" value={profileForm.phone} onChange={(e) => setProfileForm({ ...profileForm, phone: e.target.value })} className="mt-2 w-full rounded-lg border border-primary/15 px-3 py-2.5" /></label><button disabled={profileSaving} className="rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-60">{profileSaving ? "Updating..." : "Update profile"}</button></form></section>;
}

function OwnerProperties({ properties, formOpen, editingId, form, setForm, setFormOpen, saving, saveProperty, openAdd, openEdit, removeProperty }) {
  const [selected, setSelected] = useState(null);
  return <section className="space-y-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-xl font-semibold text-primary">My Properties</h2><p className="mt-1 text-sm text-primary/60">Only properties owned by your account are shown.</p></div><button onClick={openAdd} className="inline-flex items-center gap-2 rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-white"><Plus size={16} /> Add Property</button></div>{formOpen && <form onSubmit={saveProperty} className="rounded-xl border border-accent/20 bg-white p-5"><h3 className="font-semibold text-primary">{editingId ? "Edit Property" : "Add Property"}</h3><div className="mt-4 grid gap-4 sm:grid-cols-2"><input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Property name" className="rounded-lg border border-primary/15 px-3 py-2.5" /><input required value={form.location} onChange={(e) => setForm({ ...form, location: e.target.value })} placeholder="Location" className="rounded-lg border border-primary/15 px-3 py-2.5" /><input required min="1" type="number" value={form.rooms} onChange={(e) => setForm({ ...form, rooms: e.target.value })} placeholder="Number of rooms" className="rounded-lg border border-primary/15 px-3 py-2.5" /><input required value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })} placeholder="Price range" className="rounded-lg border border-primary/15 px-3 py-2.5" /><textarea value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} placeholder="Description" rows="3" className="sm:col-span-2 rounded-lg border border-primary/15 px-3 py-2.5" /></div><div className="mt-4 flex gap-2"><button disabled={saving} className="rounded-lg bg-accent px-4 py-2 text-sm font-semibold text-white">{saving ? "Saving..." : "Save"}</button><button type="button" onClick={() => setFormOpen(false)} className="rounded-lg border border-primary/15 px-4 py-2 text-sm">Cancel</button></div></form>}{properties.length ? properties.map((property) => <article key={property._id} className="rounded-xl border border-primary/10 bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><h3 className="font-semibold text-primary">{property.name}</h3><p className="mt-1 text-sm text-primary/60">{property.location} · {property.rooms} rooms · {property.price}</p><div className="mt-3 flex flex-wrap gap-2"><span className={`rounded-full px-2 py-1 text-xs font-semibold ${property.verificationStatus === "verified" ? "bg-green-100 text-green-700" : property.verificationStatus === "rejected" ? "bg-red-100 text-red-700" : "bg-amber-100 text-amber-700"}`}>{property.displayStatus}</span><span className="rounded-full bg-primary/5 px-2 py-1 text-xs font-semibold text-primary">{property.status}</span></div>{property.rejectionReason && <p className="mt-2 text-sm text-red-600">Rejection reason: {property.rejectionReason}</p>}</div><div className="flex flex-wrap gap-1"><button onClick={() => setSelected(property)} className="inline-flex items-center gap-1 rounded-lg border border-primary/15 px-3 py-2 text-xs font-semibold"><Eye size={15} /> View</button><button onClick={() => openEdit(property)} className="rounded-lg p-2 text-primary/60 hover:bg-primary/5" aria-label="Edit property"><Pencil size={16} /></button><button onClick={() => removeProperty(property)} className="rounded-lg p-2 text-red-500 hover:bg-red-50" aria-label="Delete property"><Trash2 size={16} /></button></div></div></article>) : <div className="rounded-xl border border-dashed border-primary/20 bg-white p-8 text-center text-sm text-primary/60">No properties found. Add your first property.</div>}{selected && <div className="fixed inset-0 z-40 flex items-center justify-center bg-primary/40 p-4"><div className="w-full max-w-lg rounded-xl bg-white p-6"><div className="flex items-center justify-between"><h3 className="text-lg font-semibold text-primary">Property details</h3><button onClick={() => setSelected(null)} aria-label="Close property details"><X size={18} /></button></div><p className="mt-4 text-sm text-primary/70">{selected.name} · {selected.location}</p><p className="mt-2 text-sm text-primary/70">{selected.rooms} rooms · {selected.price}</p><p className="mt-2 text-sm text-primary/70">{selected.displayStatus} · {selected.status}</p><p className="mt-2 text-sm text-primary/70">{selected.description || "No description provided."}</p></div></div>}</section>;
}

export default function OwnerDashboard() {
  const { user, updateProfile } = useAuth();
  const [tab, setTab] = useState("properties");
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");
  const [notice, setNotice] = useState(null);
  const [form, setForm] = useState(emptyForm);
  const [editingId, setEditingId] = useState(null);
  const [formOpen, setFormOpen] = useState(false);
  const [saving, setSaving] = useState(false);
  const [profileForm, setProfileForm] = useState({ name: user?.name || "", phone: user?.phone || "" });
  const [profileSaving, setProfileSaving] = useState(false);

  const loadDashboard = async () => {
    setLoading(true);
    setError("");
    try { setData(await getOwnerDashboard()); } catch (err) { setError(err.message); } finally { setLoading(false); }
  };

  useEffect(() => { loadDashboard(); }, []);
  useEffect(() => { setProfileForm({ name: user?.name || "", phone: user?.phone || "" }); }, [user]);

  const showNotice = (message, type = "success") => setNotice({ message, type });
  const properties = (data?.properties || []).map((property) => ({ ...property, displayStatus: propertyDisplayStatus(property) }));
  const inquiries = data?.inquiries || [];
  const bookings = data?.bookings || [];
  const reviews = data?.reviews || [];
  const stats = data?.stats || {};

  const openAdd = () => {
    const canManageProperties = user?.verificationStatus === "verified" || user?.ownerStatus === "Approved";
    if (!canManageProperties) {
      showNotice("Your owner account must be verified before adding a property.", "error");
      return;
    }
    setEditingId(null);
    setForm(emptyForm);
    setFormOpen(true);
    setTab("properties");
  };
  const openEdit = (property) => { setEditingId(property._id); setForm({ ...emptyForm, ...property, city: property.city || "", area: property.area || "", college: property.college || "", monthlyRent: property.monthlyRent ?? "", price: property.price || "", totalRooms: property.totalRooms || property.rooms || "", availableRooms: property.availableRooms ?? "", roomType: property.roomType || "", genderPreference: property.genderPreference || "co-living", facilities: Array.isArray(property.facilities) ? property.facilities : [], food: Array.isArray(property.food) ? property.food : [], foodIncluded: Boolean(property.foodIncluded), distanceFromCollege: property.distanceFromCollege ?? "", images: Array.isArray(property.images) ? property.images : [] }); setFormOpen(true); setTab("properties"); };
  const saveProperty = async (event) => {
    event.preventDefault();
    const totalRooms = Number(form.totalRooms);
    const availableRooms = Number(form.availableRooms);
    const monthlyRent = Number(form.monthlyRent);
    if (!form.name.trim() || !form.city.trim() || !form.area.trim() || !form.location.trim()) return showNotice("Name, city, area, and location are required.", "error");
    if (!Number.isFinite(monthlyRent) || monthlyRent <= 0) return showNotice("Monthly rent must be greater than 0.", "error");
    if (!Number.isInteger(totalRooms) || totalRooms <= 0) return showNotice("Total rooms must be a positive whole number.", "error");
    if (!Number.isInteger(availableRooms) || availableRooms < 0 || availableRooms > totalRooms) return showNotice("Available rooms cannot exceed total rooms.", "error");
    if (!form.roomType) return showNotice("Please select a room type.", "error");
    setSaving(true); setError("");
    try {
      const payload = { ...form, rooms: totalRooms, totalRooms, availableRooms, monthlyRent, price: form.price || String(monthlyRent), distanceFromCollege: form.distanceFromCollege === "" ? undefined : Number(form.distanceFromCollege) };
      if (editingId) await updateProperty(editingId, payload); else await createProperty(payload);
      await loadDashboard(); setForm(emptyForm); setEditingId(null); setFormOpen(false); showNotice(editingId ? "Property updated successfully." : "Property added successfully.");
    } catch (err) { setError(err.message); showNotice(err.message, "error"); } finally { setSaving(false); }
  };
  const removeProperty = async (property) => {
    if (!window.confirm(`Delete ${property.name}? This cannot be undone.`)) return;
    try { await deleteProperty(property._id); await loadDashboard(); showNotice("Property deleted successfully."); } catch (err) { showNotice(err.message, "error"); }
  };
  const changeInquiryStatus = async (id, status) => {
    try { await updateInquiryStatus(id, status); await loadDashboard(); showNotice("Inquiry status updated."); } catch (err) { showNotice(err.message, "error"); }
  };
  const changeBookingStatus = async (id, status) => {
    try { await updateBookingStatus(id, status); await loadDashboard(); showNotice("Booking status updated."); } catch (err) { showNotice(err.message, "error"); }
  };
  const saveProfile = async (event) => {
    event.preventDefault(); setProfileSaving(true);
    try { await updateProfile(profileForm); showNotice("Profile updated successfully."); } catch (err) { showNotice(err.message, "error"); } finally { setProfileSaving(false); }
  };

  const content = () => {
    if (loading) return <div className="rounded-xl border border-primary/10 bg-white p-10 text-center text-sm text-primary/60">Loading your owner dashboard...</div>;
    if (error && !data) return <div className="rounded-xl border border-red-200 bg-red-50 p-6 text-sm text-red-700"><p>{error}</p><button onClick={loadDashboard} className="mt-3 rounded-lg bg-red-600 px-4 py-2 font-medium text-white">Try again</button></div>;
    if (tab === "overview") return <OwnerOverview properties={properties} inquiries={inquiries} bookings={bookings} reviews={reviews} setTab={setTab} />;
    if (tab === "properties") return <OwnerPropertiesPanel properties={properties} formOpen={formOpen} editingId={editingId} form={form} setForm={setForm} setFormOpen={setFormOpen} saving={saving} saveProperty={saveProperty} openAdd={openAdd} openEdit={openEdit} removeProperty={removeProperty} error={error} />;
    if (tab === "inquiries") return <OwnerInquiries inquiries={inquiries} changeInquiryStatus={changeInquiryStatus} />;
    if (tab === "bookings") return <OwnerBookings bookings={bookings} changeBookingStatus={changeBookingStatus} />;
    if (tab === "reviews") return <OwnerReviews reviews={reviews} />;
    if (tab === "profile") return <OwnerProfile user={user} profileForm={profileForm} setProfileForm={setProfileForm} saveProfile={saveProfile} profileSaving={profileSaving} />;
    if (tab === "overview") return <>
      <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">
        <Metric icon={Building2} label="Total Properties" value={stats.totalProperties ?? 0} onClick={() => setTab("properties")} tone="bg-blue-50 text-blue-700" />
        <Metric icon={Users} label="Active Tenants" value={stats.activeTenants ?? 0} onClick={() => setTab("bookings")} tone="bg-green-50 text-green-700" />
        <Metric icon={MessageSquare} label="New Inquiries" value={stats.newInquiries ?? 0} onClick={() => setTab("inquiries")} tone="bg-purple-50 text-purple-700" />
        <Metric icon={CircleDollarSign} label="Revenue This Month" value={`₹${(stats.revenueThisMonth ?? 0).toLocaleString("en-IN")}`} onClick={() => setTab("bookings")} tone="bg-orange-50 text-orange-700" />
      </div>
      <div className="mt-6 grid gap-6 lg:grid-cols-[1.25fr_0.75fr]">
        <section className="rounded-xl border border-primary/10 bg-white p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold text-primary">Recent enquiries</h2><p className="mt-1 text-sm text-primary/60">Live activity from your properties.</p></div><button onClick={() => setTab("inquiries")} className="text-sm font-semibold text-accent">View all</button></div>{inquiries.length ? <div className="mt-5 space-y-3">{inquiries.slice(0, 5).map((item) => <div key={item._id} className="flex items-start gap-3 rounded-lg bg-primary/5 p-3"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-full bg-white text-xs font-bold text-primary">{item.tenant?.name?.slice(0, 2).toUpperCase() || "?"}</span><div className="min-w-0 flex-1"><p className="text-sm font-medium text-primary">{item.tenant?.name || "Unknown tenant"}</p><p className="text-xs text-primary/60">{item.property?.name || "Property removed"}</p><p className="mt-1 truncate text-xs text-primary/50">{item.message}</p></div><span className="text-xs text-primary/50">{item.status}</span></div>)}</div> : <p className="mt-6 text-sm text-primary/60">No enquiries yet.</p>}</section>
        <section className="rounded-xl bg-primary p-5 text-white"><div className="flex items-center justify-between"><div><p className="text-sm text-white/60">Owner performance</p><p className="mt-2 text-3xl font-bold">{stats.averageRating || "—"}</p><p className="mt-1 text-sm text-white/70">Average review rating</p></div><BarChart3 /></div><div className="mt-8 grid grid-cols-2 gap-3 text-sm"><div className="rounded-lg bg-white/10 p-3"><p className="text-white/60">Active listings</p><p className="mt-1 font-bold">{stats.activeListings ?? 0}</p></div><div className="rounded-lg bg-white/10 p-3"><p className="text-white/60">Reviews</p><p className="mt-1 font-bold">{reviews.length}</p></div></div></section>
      </div>
    </>;
    if (tab === "properties") return <section className="space-y-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-xl font-semibold text-primary">My Properties</h2><p className="mt-1 text-sm text-primary/60">Only properties owned by your account are shown.</p></div><button onClick={openAdd} className="inline-flex items-center gap-2 rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-white"><Plus size={16} /> Add Property</button></div>{formOpen && <form onSubmit={saveProperty} className="rounded-xl border border-accent/20 bg-white p-5"><h3 className="font-semibold text-primary">{editingId ? "Edit Property" : "Add Property"}</h3>{error && <p className="mt-3 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="mt-4 grid gap-4 sm:grid-cols-2"><input required name="name" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Property name" className="rounded-lg border border-primary/15 px-3 py-2.5" /><input required name="location" value={form.location} onChange={(e) => setForm({ ...form, location: e.target.value })} placeholder="Location" className="rounded-lg border border-primary/15 px-3 py-2.5" /><input required min="1" type="number" name="rooms" value={form.rooms} onChange={(e) => setForm({ ...form, rooms: e.target.value })} placeholder="Number of rooms" className="rounded-lg border border-primary/15 px-3 py-2.5" /><input required name="price" value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })} placeholder="Price range" className="rounded-lg border border-primary/15 px-3 py-2.5" /><textarea name="description" value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} placeholder="Description" rows="3" className="sm:col-span-2 rounded-lg border border-primary/15 px-3 py-2.5" /></div><div className="mt-4 flex gap-2"><button disabled={saving} className="inline-flex items-center gap-2 rounded-lg bg-accent px-4 py-2 text-sm font-semibold text-white disabled:opacity-60"><Save size={15} /> {saving ? "Saving..." : "Save"}</button><button type="button" onClick={() => setFormOpen(false)} className="rounded-lg border border-primary/15 px-4 py-2 text-sm">Cancel</button></div></form>}{properties.length ? properties.map((property) => <article key={property._id} className="rounded-xl border border-primary/10 bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div className="flex gap-3"><span className="rounded-lg bg-primary/5 p-3"><Building2 className="text-primary" /></span><div><h3 className="font-semibold text-primary">{property.name}</h3><p className="mt-1 text-sm text-primary/60">{property.location} · {property.rooms} rooms · {property.price}</p><span className="mt-3 inline-block rounded-full bg-green-100 px-2 py-1 text-xs font-semibold text-green-700">{property.status}</span></div></div><div className="flex gap-1"><button onClick={() => openEdit(property)} className="rounded-lg p-2 text-primary/60 hover:bg-primary/5" aria-label="Edit property"><Pencil size={16} /></button><button onClick={() => removeProperty(property)} className="rounded-lg p-2 text-red-500 hover:bg-red-50" aria-label="Delete property"><Trash2 size={16} /></button></div></div></article>) : <div className="rounded-xl border border-dashed border-primary/20 bg-white p-8 text-center text-sm text-primary/60">No properties found. Add your first property.</div>}</section>;
    if (tab === "inquiries") return <section className="rounded-xl border border-primary/10 bg-white p-5"><div className="mb-5"><h2 className="text-xl font-semibold text-primary">Inquiries</h2><p className="mt-1 text-sm text-primary/60">Review and update enquiries for your properties.</p></div>{inquiries.length ? <div className="space-y-3">{inquiries.map((item) => <div key={item._id} className="flex flex-col gap-3 rounded-lg border border-primary/10 p-4 lg:flex-row lg:items-center"><div className="min-w-0 flex-1"><p className="font-medium text-primary">{item.tenant?.name || "Unknown tenant"}</p><p className="text-sm text-primary/60">{item.tenant?.email || ""} · {item.property?.name || "Property removed"}</p><p className="mt-2 text-sm text-primary/70">{item.message}</p>{item.adminReply && <p className="mt-2 text-sm text-accent">Admin reply: {item.adminReply}</p>}</div><select value={item.status} onChange={(e) => changeInquiryStatus(item._id, e.target.value)} className="rounded-lg border border-primary/15 px-3 py-2 text-sm"><option>New</option><option>Viewed</option><option>Replied</option><option>Closed</option></select><a href={`mailto:${item.tenant?.email || ""}`} className="inline-flex items-center gap-1 text-sm font-semibold text-accent"><Mail size={15} /> Reply</a></div>)}</div> : <p className="py-8 text-center text-sm text-primary/60">No enquiries found.</p>}</section>;
    if (tab === "bookings") return <section className="rounded-xl border border-primary/10 bg-white p-5"><h2 className="text-xl font-semibold text-primary">Bookings & Tenants</h2><p className="mt-1 text-sm text-primary/60">Live bookings belonging to your properties.</p>{bookings.length ? <div className="mt-5 space-y-3">{bookings.map((booking) => <div key={booking._id} className="flex flex-wrap items-center justify-between gap-3 rounded-lg border border-primary/10 p-4"><div><p className="font-medium text-primary">{booking.user?.name || booking.tenant?.name || "Unknown tenant"}</p><p className="text-sm text-primary/60">{booking.property?.name || "Property removed"} · {booking.user?.phone || booking.tenant?.phone || ""}</p><p className="text-xs text-primary/50">Requested {new Date(booking.bookingDate || booking.createdAt).toLocaleDateString()}{booking.moveInDate ? ` · Move in ${new Date(booking.moveInDate).toLocaleDateString()}` : ""}</p></div><span className="rounded-full bg-primary/5 px-3 py-1 text-xs font-semibold text-primary">{booking.status}</span><strong className="text-primary">₹{(booking.amount || booking.rent || 0).toLocaleString("en-IN")}</strong>{booking.status === "Pending" && <div className="flex gap-2"><button onClick={() => changeBookingStatus(booking._id, "Confirmed")} className="rounded-lg bg-emerald-700 px-3 py-2 text-xs font-semibold text-white">Accept</button><button onClick={() => changeBookingStatus(booking._id, "Rejected")} className="rounded-lg bg-red-600 px-3 py-2 text-xs font-semibold text-white">Reject</button></div>}{booking.status === "Confirmed" && <button onClick={() => changeBookingStatus(booking._id, "Cancelled")} className="rounded-lg border border-red-200 px-3 py-2 text-xs font-semibold text-red-600">Cancel</button>}</div>)}</div> : <p className="py-8 text-center text-sm text-primary/60">No bookings or tenants found.</p>}</section>;
    if (tab === "reviews") return <section className="rounded-xl border border-primary/10 bg-white p-5"><h2 className="text-xl font-semibold text-primary">Reviews</h2><p className="mt-1 text-sm text-primary/60">Reviews for your properties.</p>{reviews.length ? <div className="mt-5 space-y-3">{reviews.map((review) => <article key={review._id} className="rounded-lg border border-primary/10 p-4"><div className="flex justify-between gap-3"><div><p className="font-medium text-primary">{review.tenant?.name || "Tenant"}</p><p className="text-sm text-primary/60">{review.property?.name || "Property removed"}</p></div><span className="font-semibold text-accent">{review.rating}/5 ★</span></div><p className="mt-3 text-sm text-primary/70">{review.comment || "No written comment."}</p></article>)}</div> : <p className="py-8 text-center text-sm text-primary/60">No reviews found.</p>}</section>;
    return <section className="max-w-2xl rounded-xl border border-primary/10 bg-white p-5"><h2 className="text-xl font-semibold text-primary">Profile</h2><p className="mt-1 text-sm text-primary/60">Update your owner account details.</p><form onSubmit={saveProfile} className="mt-5 space-y-4"><label className="block text-sm font-medium text-primary">Full name<input required value={profileForm.name} onChange={(e) => setProfileForm({ ...profileForm, name: e.target.value })} className="mt-2 w-full rounded-lg border border-primary/15 px-3 py-2.5" /></label><label className="block text-sm font-medium text-primary">Phone<input required pattern="[0-9]{10}" value={profileForm.phone} onChange={(e) => setProfileForm({ ...profileForm, phone: e.target.value })} className="mt-2 w-full rounded-lg border border-primary/15 px-3 py-2.5" /></label><p className="text-sm text-primary/60">Email: {user?.email}</p><button disabled={profileSaving} className="rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-60">{profileSaving ? "Updating..." : "Update profile"}</button></form></section>;
  };

  return <DashboardShell roleLabel="Owner"><Notice notice={notice} onClose={() => setNotice(null)} /><div className="space-y-6"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-sm text-primary/60">Owner workspace</p><h1 className="mt-1 text-2xl font-bold text-primary">Manage your PG business</h1></div><button onClick={openAdd} className="inline-flex items-center gap-2 rounded-lg bg-accent px-4 py-2.5 text-sm font-semibold text-white"><Plus size={16} /> Add Property</button></div><nav className="flex gap-1 overflow-x-auto border-b border-primary/10" aria-label="Owner dashboard tabs">{tabs.map(([id, label, Icon]) => <button key={id} onClick={() => setTab(id)} className={`inline-flex shrink-0 items-center gap-2 border-b-2 px-3 py-3 text-sm font-medium ${tab === id ? "border-accent text-accent" : "border-transparent text-primary/60 hover:text-primary"}`}><Icon size={16} />{label}</button>)}</nav>{content()}</div></DashboardShell>;
}
